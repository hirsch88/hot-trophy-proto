describe('Midway: myTournaments', function () {

  var module;
  before(function () {
    module = angular.module('myTournaments');
  });

  it('should be registered', function () {
    expect(module).not.to.equal(null);
  });




});
