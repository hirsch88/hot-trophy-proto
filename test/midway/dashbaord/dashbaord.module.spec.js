describe('Midway: dashbaord', function () {

  var module;
  before(function () {
    module = angular.module('dashbaord');
  });

  it('should be registered', function () {
    expect(module).not.to.equal(null);
  });




});
