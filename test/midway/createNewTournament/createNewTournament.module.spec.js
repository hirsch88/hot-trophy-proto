describe('Midway: createNewTournament', function () {

  var module;
  before(function () {
    module = angular.module('createNewTournament');
  });

  it('should be registered', function () {
    expect(module).not.to.equal(null);
  });




});
