describe('Midway: login', function () {

  var module;
  before(function () {
    module = angular.module('login');
  });

  it('should be registered', function () {
    expect(module).not.to.equal(null);
  });




});
